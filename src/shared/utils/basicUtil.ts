export const scrollToTop = () => {
    window.scrollTo(0, 0);

    /* const anchor = document.querySelector('#back-to-top-anchor');

    if (anchor) {
        anchor.scrollIntoView({
            block: 'center',
        });
    } */
};