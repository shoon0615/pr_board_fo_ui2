## package.json 백업(20250529)
```json
"scripts": {
    // webpack 설정 변경으로 인해 CRA -> craco 로 변경
    /* "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject" */
    "start": "craco start",
    "build": "craco build",
    "test": "craco test",
    "eject": "craco eject"
  },
```

